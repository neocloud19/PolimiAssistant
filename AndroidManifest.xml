<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.PolimiAssitant"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk android:minSdkVersion="11"
            android:targetSdkVersion="19"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application android:label="@string/app_name"
                 android:icon="@drawable/ic_launcher"
                 android:theme="@style/PolimiMobile">

        <!-- Splash screen -->
        <activity android:name=".SplashScreen"
                  android:label="@string/layout_title_splashscreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- Main screen -->
        <activity android:name=".MainMenu"
                android:label="@string/layout_title_menu"
                android:parentActivityName=".LoginScreen">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="com.example.PolimiAssitant.LoginScreen" />

        </activity>

        <!-- Login screen -->
        <activity android:name=".LoginScreen"
                android:label="@string/layout_title_login"
                android:parentActivityName=".SplashScreen">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="com.example.PolimiAssitant.SplashScreen" />

        </activity>

    </application>

</manifest>
